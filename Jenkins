
pipeline {
    agent {
        docker { image 'ubuntu:16.04'}
    } 
    stages {
        stage('test') {
            steps {
                sh '''
                author=`cd jenkins_test && git --no-pager show -s --format=\'%an\'`
                echo "${author}"
                '''
            }
        }
    }
}
