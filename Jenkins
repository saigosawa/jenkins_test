
pipeline {
    agent {
        docker { image 'ubuntu:16.04'}
    } 
    stages {
        stage('test') {
            steps {
                sh '''
                cd jenkins_test
                author=`git --no-pager show -s --format=\'%an\'`
                echo "${author}"
                '''
            }
        }
    }
}
